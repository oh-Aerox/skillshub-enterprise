{
  "name": "skillshub-enterprise",
  "version": "1.0.0",
  "description": "Enterprise AI Skills Private Registry Platform",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:web\"",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "lint": "eslint apps/web/src"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
